services:
  hermes-bot:
    build: .
    container_name: hermes-bot
    restart: unless-stopped
    mem_limit: 700m
    memswap_limit: 700m
    env_file:
      - .env
    volumes:
      - ./data:/app/data
